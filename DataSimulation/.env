# Kafka bootstrap (matches your docker hostnames)
KAFKA_BOOTSTRAP=kafka:9092

# Telemetry topic
DEVICE_READINGS_TOPIC=device-readings
DEVICE_READINGS_PARTITIONS=3

# Simulator knobs
SIM_DEVICES=100
SIM_SPEEDUP=600
SIM_HOURS=6
SIM_PREFIX=device-
SIM_SEED=42
ACKS=0
